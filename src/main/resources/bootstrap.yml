server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.kelivan.**.model
